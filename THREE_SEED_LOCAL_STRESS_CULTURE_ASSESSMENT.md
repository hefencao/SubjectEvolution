# 三 seed 局部压力与文化传播评估

## 输入

用户以 `--backend gpu`、strict-reference 配置完成三个 seed、1500 ticks。附件分析包含 50 个 30-tick 窗口和 16 个分析区域。

注意：该配置的 `gpu_semantics_mode` 为 `strict-reference`。在当前项目定义中，成功的 GPU 请求会验证 CUDA/CuPy 可用性，但权威世界语义仍为 CPU reference；它不是 `hybrid-accelerated` 性能结果。旧 v4 分析文件未保存 manifest 后端字段，v5 已补充该输出。

## 跨 seed 稳定局部关系

### 资源稀缺

同一区域内：

- scarcity → cohesion：`0.3525 / 0.3679 / 0.4217`；
- scarcity → next-window cohesion：`0.3393 / 0.3459 / 0.3809`。

这是当前最稳定的局部方向。它支持“某一区域随时间变稀缺时，内部利益留存通常更高”，但仍可能受到区域性状组成、迁移和群组持续性的共同影响。

### 拥挤

同一区域内：

- crowding → cohesion：`-0.3032 / -0.4011 / -0.3826`；
- crowding → next-window cohesion：`-0.2048 / -0.2312 / -0.2850`。

当前世界中，拥挤与凝聚度稳定负相关，不能把所有压力统一解释成“抱团取暖”。

### 死亡与危险

局部死亡只有弱正相关；危险暴露接近零。全局“生存压力”应拆成稀缺、死亡、危险和拥挤分别研究。

## 局部异质性

- 区域人口 CV：`0.125–0.135`；
- 区域死亡压力 CV：`0.653–0.668`；
- 最高局部/全局死亡率：`5.04–7.40×`；
- 约 `7.5%–7.6%` 的区域窗口死亡率超过全局值两倍。

因此全局稳定确实掩盖了局部压力，但长期结果比 120-tick 小样本温和。

## 文化传播

三个 seed 累计成功传播 `12,208 / 12,710 / 14,428` 次，有效 transferred roots 为 `2,066.5 / 2,143.2 / 2,243.8`。宏观人口与策略差异较小，因此文化传播的价值应在区域建立、跨区扩散和局部压力事件中测量。

## v0.20 方向

1. 对成功 transfer 增加源/目的区域审计；
2. 记录区域文化根建立、消失和跨区存在；
3. 使用局部稀缺/拥挤/死亡事件选择 checkpoint；
4. 从同一 checkpoint 配对关闭未来传播或知识 residual；
5. 不合成设计者加权的总压力分数；
6. 不增加新的主体控制结构。
