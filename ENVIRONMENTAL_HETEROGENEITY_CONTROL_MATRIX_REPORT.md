# v0.15.0 环境异质性短周期对照报告

## 实验设计

三组条件均使用 CPU strict-reference、seed 10001、120 ticks，并各运行两次。知识、L2、工作记忆、遗传 Top-k 和成本设置相同。环境异质性与资源亲和通过独立 schema 开关。

| 条件 | Alive | Births | Deaths | Mean energy | 采集能量/采样存活量 | 活跃形态有效维度 | 亲和有效维度 | 窗口动作熵 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 旧环境基线 | 1222 | 760 | 38 | 1.7415 | 0.043462 | 1.975 | 0.000 | 1.9684 |
| 多资源、无亲和 | 1020 | 556 | 36 | 2.0059 | 0.043872 | 1.961 | 0.000 | 1.9659 |
| 多资源、固定预算亲和 | 1002 | 540 | 38 | 2.0621 | 0.042770 | 5.719 | 2.965 | 1.9646 |

## 主要结果

1. 旧环境中只有传感器质量和速度两个形态性状参与该诊断，活跃形态有效维度为 `1.975`。
2. 在多资源世界启用固定总预算亲和后，活跃形态有效维度为 `5.719`，四资源亲和子空间有效维度为 `2.965`。
3. 最终预算配置下，多资源无亲和与亲和条件的采集能量/采样存活量分别为 `0.043872` 和 `0.042770`；旧环境为 `0.043462`。三者已处于相近量级。
4. 人口轨迹仍不相同：多资源条件最终约 1000，而旧环境为 1222。这不能简单视为预算失配，因为空间异步、局部可达性和五维收益分配也会改变繁殖与死亡时序。
5. 亲和开启与关闭的最终人口接近，但亲和开启显著增加了活跃形态维度；这支持“新增环境轴被主体使用”，尚不支持长期多样性已被维持。

## 确定性

三组双重复在排除 12 个计时字段后，所有 268 个共同 metrics 字段一致；核心 JSONL/CSV 日志 byte-identical；tick 60/120 的 38 个共同 checkpoint 数组逐数组一致。

## 校准说明

第一版多资源矩阵使世界整体明显更富裕，120 tick 人口约 1650；过低矩阵则使人口降至约 940。最终矩阵以单位存活采集能量接近旧环境为准，而不是强行把人口调成相同数值。这样避免用目标人口反向塑造生态结果。

## 不能得出的结论

- 120 tick 不能证明多资源环境会阻止 3000 tick 的谱系垄断；
- 不能证明亲和性状具有长期适应优势；
- 不能证明群组凝聚度会提高；
- 不能证明真实 GPU 与 CPU 完全一致。

下一步应使用 3–5 seeds、至少 1–2 个旧环境周期长度的运行，重点比较亲和性状选择差、有效形态维度、有效知识谱系和生态相位下的存活差异。
