# v0.15.0 环境异质性与多资源生态位实现说明

## 目标

v0.14 长周期报告表明，主体结构已经能够长期运行并产生选择，但旧世界中真正参与生态权衡的形态性状过少。v0.15 不继续增加路由网络深度，而是增加公开、可配置、无群体奖励预设的环境维度，让既有主体能力面对多个真实权衡。

## 新增 Schema

- 环境：`spatially-asynchronous-multiniche-v1`
- 资源亲和：`normalized-four-resource-affinity-v1`
- 旧环境：`legacy-four-channel-v1`，语义保持不变

## 四资源环境

四个资源通道分别拥有可配置的再生、容量、季节时间相位和空间相位。季节变化不是全局同步开关；不同区域和不同通道处于不同相位，降低全世界同时繁荣或同时衰退的单一瓶颈。

每个资源通道通过公开的 `4 x 5` 效果矩阵映射到五维身体后果：能量、完整性、材料、信息和繁殖机会。矩阵属于实验世界规则，并写入 resolved config；没有根据谱系、群组或目标多样性动态发奖。

## 固定总预算亲和性状

形态基因 1–4 在新 schema 下成为四资源亲和性状。量化亲和满足：

```text
affinity_q[0] + affinity_q[1] + affinity_q[2] + affinity_q[3] = 4 × 4096
```

提高一个通道的效率必然占用其他通道预算，因此不存在把四个基因全部调高的免费优势。基因 0 的传感器质量和基因 5 的速度语义保持不变。旧 schema 下基因 1–4 继续不参与该机制。

亲和影响：

1. 主体对本地四资源的策略效用；
2. 多资源梯度方向；
3. 实际采集后的五维身体效果。

权威环境资源仍保存为原始四通道 SoA；策略效用视图不会改写环境状态。

## GPU 与确定性

- CPU 与 NumPy device backend 的异质环境字段和梯度已有专项一致性测试；
- 亲和预算使用整数规范化；
- legacy 分支保留原始运算顺序；
- hybrid GPU 代码路径已接入多资源效用与梯度；
- 当前环境无 CUDA，真实 CuPy 多 tick world parity 尚未验证。

## 科学边界

本实现没有：

- 给稀有基因或稀有谱系额外奖励；
- 惩罚大谱系；
- 惩罚跨群分享；
- 预设群内合作应当更优；
- 引入全局类别 embedding、Softmax Attention 或外部训练器。

环境异质性只是扩大可发生选择的公开物理维度，不保证多样性一定提高。
