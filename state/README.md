# Workspace state

Mutable local overlays belong here. The current decision overlay is
`state/decisions/exploration_candidate_ledger.json`; immutable release history
remains under `protocols/decisions/` and in the installed package resource.
