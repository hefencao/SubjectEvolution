# Runs

All executable simulation outputs and checkpoints belong here.

- `base/`: uninterrupted source trajectories and shared checkpoints;
- `interventions/`: baseline/intervention branch executions.

Run directories are local artifacts and are ignored by Git. Plans use
workspace-relative paths so a checkout can be moved without rewriting them.
