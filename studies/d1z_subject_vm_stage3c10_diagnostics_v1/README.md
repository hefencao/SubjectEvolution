# D1-Z Subject VM Stage 3C-10 short paired diagnostics

This workflow reuses the exact v0.123 Stage 3C-9 study factors: seeds `12301,12302,12303`, source tick `2`, branch horizon `5`, sixteen fixed-bootstrap subjects, CPU backend and disabled permanent retention. It changes no learning mechanism or experimental variable.

Stage 3C-10 asks where the short-study signal becomes sparse. It reports the source/subject funnel from token emission through completed windows, explicit rejection reasons, update size and temporary exposure, eligibility and association distributions, target reuse, live/control admission and counted-cost symmetry, first branch divergence, exact parameter restoration, post-rollback path dependence and diagnostic alternatives to the primary source-balanced aggregation.

The fixed graph remains a `fixed-cognition engineering shaping aid`; `evolved_topology=false` and `universal_attention_claim=false`. Similarity is addressing metadata, not causal credit or value. The workflow produces no scalar objective, automatic keep/revert decision, permanent retention authorization, learning claim or subjecthood claim.
