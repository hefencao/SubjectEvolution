# 设计边界

## 空间层

- `terrain`：只表示移动阻力及其能耗影响。
- `signal_openness`：只表示当前抽象信号载体的传播开放度。
- 两个字段分别生成、分别配置；不存在固定符号关系。
- D1-V 调试图故意让高移动阻力与高信号开放度同相，以验证“难走但看得远”可以表达。这只是一个地图实例，不是普遍规律。

## 直接冲突

D1-V 不引入直接冲突执行器。直接冲突必须在后续以版本化显式动作接入，不能复活相邻采集自动造成身体损伤的隐式语义。详细要求见 `DIRECT_CONFLICT_ASSESSMENT.md`。
