# D1-H demand-gated resource sensing

Use `se-study show studies/d1h_demand_gated_resource_sensing_v1` to inspect the
exact argv and all overridable parameters before execution. Run outputs remain
under `runs/` and `analyses/`; compact review bundles are written only to the
external result directory configured by `se-study config --set-result-dir`.
