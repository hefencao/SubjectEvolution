# Stage 3C-29 transition-class and opportunity-conditioned basin audit

This workflow reruns the frozen Stage 3C-23 rank-one and rank-two arms,
restores the complete Stage 3C-23 through Stage 3C-28 lineage, and analyzes
only the rank-two read-only control checkpoints. It excludes forced
single-candidate assignments from conditioned screens, compares exact
first-state transition-class opportunity with winner occupancy, and measures
subject-anchored second-coordinate locality under the same candidate set.

It changes no runtime mechanism, token geometry, similarity, threshold,
candidate cardinality, tie-break, update scale, rollback or retention policy.
Queries, candidate events, subjects and windows remain nested observations;
independent pre-bootstrap source checkpoints remain the highest replicate.
