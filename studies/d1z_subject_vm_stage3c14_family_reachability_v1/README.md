# D1-Z Subject VM Stage 3C-14

This workflow runs a nine-source, eight-tick, exposure-three CPU comparison between one-hot fixed-bootstrap routing to `node_bias` and `node_output_gate`. It preserves the source panel, bounded delta, rollback, evaluation, topology size and objective aggregation.

The result bundle contains both Stage 3C-7/8/10 evidence sets and the Stage 3C-14 reachability assessment. Checkpoints and ordinary run directories are excluded from the packaged research artifact.
