# D1-Z Subject VM Stage 3C-18

This workflow compares a single latest-ranked association reference with an at-most-two latest-ranked allocation on the frozen nine-source carrier-on `edge_forward_gate` baseline. The two-candidate arm combines objective-fact vectors with an equal-weight mean before the existing normalized contrast, so each current event still produces at most one modulation proposal and remains under the same update budget. The result package excludes checkpoints and ordinary run directories.
