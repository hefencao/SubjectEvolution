# D1-Z Subject VM Stage 3C-20

This workflow compares the frozen constant-anchor visible token with one explicit graph-state readout on port 29. It verifies that source checkpoints and read-only objective behavior remain identical, then reports token geometry, score spread, selected delay, funnel coverage and Stage 3C-7/8 evidence. The result bundle excludes checkpoints and ordinary run directories.
