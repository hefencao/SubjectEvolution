# D1-Z Subject VM Stage 3C-19

This workflow regenerates the frozen nine-source carrier-on top-1 baseline and performs an external read-only diagnostic of association-visible token geometry. It does not alter token production, normalized-dot similarity, candidate allocation, update scale, rollback, retention or topology. The result package contains only declared reports and excludes checkpoints and run directories.
