# D1-Z Subject VM Stage 3C-11 independent-source adequacy audit

This workflow answers whether the original `32`-entity, `5`-tick, three-source study was only an engineering pilot or already an adequate sample for cross-source direction claims.

It keeps all runtime and experimental factors fixed and expands only the independent source set from seeds `12301,12302,12303` to `12301..12309`. It then runs the unchanged Stage 3C-7, Stage 3C-8 and Stage 3C-10 pipeline and adds a Stage 3C-11 source-level adequacy report.

The report preserves the hierarchy `window -> stable subject -> independent source`. It does not count 32 entities, 16 bootstrap subjects or 111 paired windows as 111 independent replicates. The fixed bootstrap remains a replaceable `fixed-cognition engineering shaping aid`; `evolved_topology=false` and `universal_attention_claim=false`.
