# Stage 3C-21 subject/event-specific objective-input readout audit

This workflow compares one common readout-only bootstrap node fed by objective
input port 0 (`constant-one`) or port 11 (`uncertainty-mean`).  The readout has
no action output and emits only to association-visible token port 29.  The
comparison does not assign value to uncertainty, change similarity or candidate
cardinality, retain parameters, or claim a general attention mechanism.
