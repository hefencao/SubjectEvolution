# D1-Z Subject VM Stage 3C-15

This workflow regenerates the frozen nine-source `node_bias` baseline and runs an external six-family finite-difference audit at the first post-bootstrap activation and a one-tick warmed delayed-edge context.

The result bundle contains the baseline Stage 3C-7/8/10 evidence and the Stage 3C-15 sensitivity assessment. Temporary replay directories, checkpoints and ordinary run outputs are excluded from the packaged research artifact.
