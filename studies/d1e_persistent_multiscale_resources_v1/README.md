# D1-E persistent multiscale resources

This study is the active ecosystem-construction step. It pairs the inherited
resource-sensing capability with a persistent four-channel environment whose
resource fields occupy distinct spatial scales.

Design and protocol are in this directory. Execute only the numbered scripts in
`commands/`. Package the files required for the next iteration with
`commands/90_pack_results.sh`.
