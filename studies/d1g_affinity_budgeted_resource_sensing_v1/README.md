# D1-G affinity-budgeted resource sensing

See `DESIGN.md` for the scientific boundary and `workflow.toml` for all
inspectable, parameterized execution steps. Result bundles are written to the
project-external directory configured with `se-study config --set-result-dir`.
