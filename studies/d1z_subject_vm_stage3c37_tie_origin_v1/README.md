# Stage 3C-37 near-exact tie origin audit

This read-only audit deterministically reconstructs the frozen rank-two checkpoints for
both source panels and compares the Stage-3C-27 `1e-8` diagnostic bin with the actual
runtime `1e-12` score comparator. It records raw float32 visible coordinates, float64
runtime scores, direct-float32 counterfactual scores, normalized directions and whether
latest-on-tie changes the selected identity.

The study may correct the qualification interpretation, but it may not rewrite the
historical Stage-3C-27 assessment, change runtime selection semantics or execute the
blocked crossing replication in the same stage.
