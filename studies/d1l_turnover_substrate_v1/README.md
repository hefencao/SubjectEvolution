# D1-L healthy generational-turnover substrate qualification

D1-L adds no gene and authorizes no paired effect. It exists to prevent another
capability from being attached to a collapsing founder-dominated population.

Execution order is `prepare-config` → `source-pilot` → `source-health`.
A failed gate terminates the chain. No longer horizon, paired branch, or gene
interpretation is authorized until all three seeds pass the staged contract.
