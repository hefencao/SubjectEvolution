# D1-Z Subject VM Stage 3C-17

This workflow compares latest-on-tie and oldest-on-tie allocation on the frozen nine-source carrier-on edge-forward baseline. Both arms retain the same normalized-dot score and candidate set. The result package excludes checkpoints and ordinary run directories.
