# K4 短周期对照报告（v0.8.0）

## 目的

验证三件事：

1. K4 能从 K1–K3 数据形成内容谱系、候选节点、边界流、成本和策略影响诊断；
2. 开启候选 tracking 不改变世界、动作或知识状态；
3. 结果在固定 seed 下可重复。

本报告不检验长期适应优势或主体性。

## 公共设置

```text
backend: CPU
seed: 10001
ticks: 30
initial_entities: 500
max_entities: 768
checkpoint ticks: 15 / 30
```

每个主要条件独立运行两次。

## 条件结果

| 条件 | alive | 出生 | 内容 | 变体 | 副本 | 宿主 | K3 影响 entity-ticks | 非零 action cells | 改变动作 | 传输提交 | 候选节点 | 活跃候选 | 多宿主候选 | 候选归因成本 | 运行时间 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| K3 private | 609 | 109 | 2,245 | 0 | 2,325 | 602 | 10,041 | 15,493 | 31 | 0 | 0 | 0 | 0 | 0 | 1.94s |
| K3 costed exchange | 608 | 108 | 2,225 | 6 | 2,323 | 601 | 10,362 | 15,855 | 29 | 224 | 0 | 0 | 0 | 0 | 1.95s |
| K4 candidate tracking | 608 | 108 | 2,225 | 6 | 2,323 | 601 | 10,362 | 15,855 | 29 | 224 | 2,225 | 2,060 | 189 | 8.157048 | 3.38s |

## 纯观察兼容性

K3 costed exchange 与 K4 candidate tracking 使用相同世界规则，仅候选 tracking 开关不同。比较结果：

- 7 行共同非计时 metrics 全部一致；
- `knowledge_events.jsonl` 一致；
- `knowledge_outcome_updates.csv` 一致；
- `knowledge_policy_contributions.csv` 一致；
- `knowledge_transfers.csv` 一致；
- `evolution_progress.jsonl` 一致；
- tick 15、30 的 32 个共同 checkpoint 数组全部一致；
- K4 仅额外写入 20 个候选诊断数组。

因此在本测试范围内，K4 是纯观察层，没有改变动作、知识更新、出生死亡或世界状态。

## 成本归因守恒

世界知识成本：

| 分量 | 能量 |
|---|---:|
| 维持 | 5.650368 |
| 发送 | 0.787360 |
| 接收 | 0.107520 |
| 学习/验证 | 1.611800 |
| 总计 | 8.157048 |

候选内容归因总计为 `8.157048000000001`，与世界知识成本之差约为 `3.91e-14`，通过浮点守恒检查。

## 高损坏率变体场景

20 ticks、初始 120 个实体的小场景用于触发知识损坏谱系：

- 候选内容：556；
- 根内容：495；
- 变体内容：61；
- 活跃内容：490；
- 非活跃历史内容：66；
- 多宿主候选：14。

两次独立运行的全部 K4 文件和 tick 10/20 的 52 个 checkpoint 数组一致。

## 边界流场景

30 ticks 的快速群组更新场景产生 6 个群组，并观察到：

- 群内成功传播：118；
- 跨群成功传播：58；
- 群组未知传播：74；
- 有效群组边界内聚：`118 / (118 + 58) = 0.6704545`。

这证明实现允许跨群传播，并分别记录内部、跨边界和未知流；内聚不是由“仅群内交换”规则强制产生。

## 确定性

K3 private、K3 exchange 和 K4 candidates 的 A/B 复跑均满足：

- 非计时 metrics 一致；
- 事件/知识/策略贡献日志一致；
- checkpoint 逐数组一致。

测试汇总：`36 passed, 1 skipped`。跳过项为当前容器无 CUDA/CuPy 的真实 GPU 测试。

## 解释限制

- 30 ticks 和一个 seed 只验证实现和确定性；
- 宿主持有者与非持有者的五维差异是观察关联，不是因果效果；
- 候选内容没有独立执行器；
- 候选分量没有被合成为主体性真值；
- K4 真实 GPU 宿主诊断路径尚未在当前容器执行。
