# v0.76 implementation report

## Supplied result

D3-R passes all source and direct-manipulation checks but fails the preregistered practical-effect gate. Its eight seed effects contain six decreases and two increases; the seed-level median relative change in cumulative oxygen uptake is approximately -0.011%, versus a 2% threshold. The candidate is terminal and does not enter replication.

## Family funnel

The candidate ledger is upgraded to `paired-exploration-candidate-ledger-v4`. A manipulation-confirmed bounded-path negative now requires an aggregate family gate before another bounded candidate can be planned in the same family revision. This prevents open-ended component fishing after a proximal path has already failed its practical-effect gate.

The D3-R portfolio metadata is regrouped under `functional-modules` revision 1. This changes only non-inferential portfolio organization; its intervention, metric, threshold, horizon and manipulation contract are unchanged.

## Next candidate

D3-S is the aggregate functional-module gate. It neutralizes all functional-module phenotype outputs and tests cumulative total harvest over the same fixed 120-tick window. A manipulation-confirmed terminal negative closes the current family revision.

Runtime reports now include the current functional-module changed-entity fraction and residual effective dimensions so the aggregate intervention is directly testable in the self-contained paired result.

No world mechanism, cost, inheritance rule, random stream, source checkpoint or large-run policy is changed.
