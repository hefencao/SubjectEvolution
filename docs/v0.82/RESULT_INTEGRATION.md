# v0.82 result integration

The supplied compact archive contains the complete frozen D3-T screen,
replication, and confirmation chain. Its confirmation stage records eight
eligible seeds, complete target engagement, one-direction effects in every
seed, an equal-seed median relative effect of about +2.827%, and terminal
`confirmed-acute` status.

The archive is sufficient for project decision continuity because the receiving
v0.81 project already contains candidate and source-protocol files whose content
hashes match the frozen stage locks. The archive does not contain raw
checkpoints. Those are not required to accept the frozen decision, but they
would be required for exact replay or a new secondary analysis that is absent
from the compact evidence.

The initial v0.81 verification failure concerned only the stale two-stage
`RUN_CHAIN.md`. That file is a derived summary, not an independent empirical
artifact. v0.82 validates the frozen evidence first and regenerates the summary
deterministically during atomic import.

D3-T is now terminal as an acute mechanism result. The result confirms repeated
suppression of realized conversion by the active support surface on the
registered fixed-checkpoint estimand. It does not identify the detailed cause of
that suppression and does not authorize a selection claim.
