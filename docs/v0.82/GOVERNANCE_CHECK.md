# v0.82 governance check

## Data versus expectation

The supplied confirmation is internally consistent with screen and replication.
The apparent project mismatch came from a stale derived run-chain summary, not
from missing empirical evidence or a failed manipulation. The task therefore
changes from running confirmation to importing its terminal result and making
result transport atomic.

## Long-term principles

Compact decision evidence and raw replay material are now separate capability
levels. Imports are append-only, content-addressed, and atomic. A terminal stage
dominates earlier promotions when candidate openness is calculated.

## Chat-only principles

The requirement that a frozen result summarize the complete design and run
chain is now enforced through the study bundle, deterministic export manifest,
chain verification, and regenerated `RUN_CHAIN.md`. Exact commands remain in
numbered files only.

## Scientific task

D3-T is not rerun and its threshold, horizon, cost, seeds, and checkpoints are
unchanged. A later experiment requires a distinct preregistered estimand and
direct manipulation contract; no explanation is selected solely from the sign
of the acute effect.
