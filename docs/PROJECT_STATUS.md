# SE project status

Version: **0.79.0**

## Current result

The supplied v0.78 portfolio audit is valid. Immutable and workspace ledgers both contain the established five decisions, no conflicts exist, and no shipped v0.78 candidate was open. The `scientific-revision-required` state is therefore a real portfolio boundary rather than a stale-history or reporting defect.

v0.79 defines D3-T as the next preregistered candidate. It does not reopen `knowledge-policy` or `functional-modules`, and it does not rerun D3-O through D3-S.

## D3-T mechanism family

- family: `spatial-processing-support`;
- revision: 1;
- role: aggregate path gate;
- intervention: `neutralize-spatial-processing-support`;
- direct interface: demand-weighted absolute deviation of local processing support from 1.0 on requested conservative conversion flow;
- primary metric: cumulative `resource-converted-total`;
- response window: 120 ticks;
- practical-effect threshold: 2%;
- independent unit: seed.

The intervention removes only the phase-shifted support multiplier. It preserves genotype, inherited storage and conversion capacities, current stores, resource fields, residue, per-unit conversion cost, checkpoint state and random streams.

## Mechanism-family state

- `knowledge-policy` revision 1: closed by D3-Q;
- `functional-modules` revision 1: closed by D3-S;
- `resource-affinity` revision 1: D3-O terminal, with no replacement specification;
- `spatial-processing-support` revision 1: open through preregistered D3-T pending screen assessment.

The effective portfolio state after adding D3-T is `candidate-specs-awaiting-assessment`.

## Current exploration policy

```text
candidate specification
→ fixed checkpoint matched screen
→ direct exposure and intervention assessment
→ seed-level practical-effect gate
→ bounded negative requires aggregate family gate
→ aggregate terminal negative closes the family revision
→ reopening requires higher revision + rationale + new measurable interface
→ immutable terminal history survives clean packaging and workspace resets
→ no automatic replacement-candidate selection
→ disjoint-seed replication only after promotion
```

## Current task

Run the D3-T eight-seed screen from the existing D3-N tick-480 shared checkpoints. Record the assessment before any replication decision. Do not reinterpret a successful manipulation check as a downstream benefit, and do not alter the 2% threshold or 120-tick horizon after observing the screen.

## Still incomplete

- D3-T paired screen assessment;
- a replicated paired acute effect for any open candidate;
- a stable common post-bottleneck source rule;
- confirmation-level long-horizon selection evidence;
- causal decomposition of founder-lineage contraction;
- device-resident action settlement, lifecycle and graph updates.
