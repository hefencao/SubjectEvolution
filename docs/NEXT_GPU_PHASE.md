# 下一阶段：GPU迁移顺序

v0.2 已在 CPU 参考内核中固定了提案→意图→结算→提交边界、稳定排序键、消息队列、候选主体图和阶段耗时。v0.4 已将前 5 个阶段和阶段 6 的采集子计划接入显式 GPU 混合路径，并将阶段 J 固化为后端无关的 `ActionResolutionSnapshot → ActionResolutionPlan` 协议。GPU实现应逐阶段复用这些语义，而不是重新定义行动或随机过程。

GPU版本不得一次性重写全部逻辑。建议按以下顺序迁移，并在每步与CPU参考输出比较：

1. 无状态随机键和基础分布；（v0.3 已完成；v0.4 已融合 GPU SplitMix64 键生成并保持位级对照）
2. 环境资源与信息场；（v0.3 已完成；v0.4 使用后端无关的稀疏 `SignalEmissionPlan`，高频事件可追加到低频目标通道的待发队列，到期时合并而不创建零列）
3. 规则网格、排序和伙伴采样；（v0.3 已完成）
4. 观察构建；（v0.4 已完成；阶段 8.5 将 CPU 批量解码结果改为稀疏 `DirectMessageObservationPlan`，GPU 只为实际接收者构造紧凑固定槽）
5. 参数化策略批处理；（v0.4 已完成）
6. 移动与采集冲突；（采集设备归约和 `GpuActionConflictResolver` 的只读采集计划已完成；稳定实体/社会字段设备缓存。移动当前没有共享冲突，仍由统一 CPU 提交写位置）
7. 分享、关系事件和固定槽位；（`ShareResolution → RelationUpdatePlan → 固定槽提交` 边界已完成；事件含来源/方向/tick，CPU 固定槽应用已优化，尚未迁移设备驻留关系表）
8. 出生死亡与空闲槽位；（`BirthRequestPlan → BirthAllocationPlan → CPU commit` 与 `DeathEventPlan → CPU commit` 已完成；槽池版本令牌拒绝陈旧计划，尚未迁移设备驻留槽池）
9. 插入性能阶段 8.5：直接消息观察与传输审计；（已完成；CPU 固定槽语义保留，GPU 不再上传 `active × capacity` 零填充张量，整步显式 H2D/D2H 字节进入指标）
10. 阶段 9：群体标签传播；（下一阶段拆为关系/成员快照、标签与成员分段计划、主体图提交；不得直接以固定上限 SoA 重写历史图）
11. 阶段 10：指标归约和异步日志。

阶段 6–10 的任何后端实现都只能读取快照、写入计划或事件缓冲；不得让策略或行动内核直接改写世界。这样可同时支持严格 CPU、设备驻留、分布式分区和离线回放，而不会把当前单 GPU 优化固化为架构约束。

多个控制器也必须先形成 `ControlProposalBatch`，再由仲裁器产生统一决策；不得把社会、谱系或 Hero 控制逻辑嵌入某个动作内核。当前已实现默认关闭的双提案社会方向控制：它只混合既有 `MOVE_RESOURCE` 方向，并以完整贡献数组记录身体和社会来源。方向混合是显式建模规则；它不等同于动作级多控制器理论的证明。

下一步进入阶段 9。第一步建立只读关系/活动实体快照；第二步输出规范标签和一次分段的 group/member 计划，消除候选主体图按每个 group 重复扫描全部 active slots；第三步由主体图单写者提交节点、成员边和增量 summary。完成 CPU 计划对照后，才决定标签数组、关系槽与生命周期 SoA 是否联合设备驻留。阶段 8.5 已证明优先收窄真实边界比单搬小核更有效：100k/300 tick 严格 GPU 两次为 `25.55/27.14秒`，相对阶段 8 最快 `30.95秒` 改善约 `12.3%–17.4%`，action counts 和最终离散状态保持一致。阶段 8 的出生规划与死亡扫描仍约为低毫秒/亚毫秒量级，不单独迁移。关系固定槽应用的 43,750 事件微基准约 `5.94ms`；设备方案必须连同持久关系槽和低频群体读取边界设计。信息场扩展应先增加显式 `InformationChannelSpec`（观察暴露、发射 cadence、衰减/扩散与传播语义），再支持可变通道数。多提案仲裁可在现有 `ControlProposalBatch` 协议上独立推进，但任何动作重采样必须先明确共享 mask、贡献归因和固定随机键语义。

每一阶段必须提供：

- CPU/GPU固定种子小场景；
- 随机分布统计对比；
- 离散事件一致或明确的统计容差；
- 性能剖析；
- 不允许策略读取新增的GPU内部真值。
