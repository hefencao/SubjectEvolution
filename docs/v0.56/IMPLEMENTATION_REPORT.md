# v0.56 implementation report

## Inputs

- Official baseline: v0.55 complete project package.
- Supplied D3-C results: three seeds, 1500 ticks.
- User-reported editable verification mismatch despite current source version.

## Findings

1. D3-C material recycling closes and should be retained.
2. Long-run external resource fields collapse to about 1.11–1.19 effective dimensions with about 0.88–0.92 mean absolute channel correlation.
3. The v1 resource geometry is only an initial condition; uniform-capacity logistic renewal does not preserve it.
4. The repeated package mismatch is a timestamp-based stale `.pyc` collision, not a second missed source edit.

## Changes

- Added bytecode cleanup before editable installation and before verification import.
- Added static-source/runtime/metadata/editable-root version checks and exact stale-bytecode regression coverage.
- Added opt-in persistent moving-target renewal with explicit abiotic source and sink accounting.
- Added CPU/device parity, open external resource ledger, D3-D experiment runner, config, reporting and protocol audit v24.
- Preserved old environment-v1/resource-v6 state and outputs exactly when D3-D is disabled.

## Scientific boundary

D3-D maintains role-free external opportunity axes. It does not name resources, protect diversity, use population feedback, or establish migration, coexistence, collection-processing specialization or trophic structure.
