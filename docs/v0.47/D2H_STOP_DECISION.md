# D2-H redesigned-source stop decision

Input assessment schema: `d2-source-population-causal-assessment-v1`

- current horizon: 120 ticks;
- selected phase: peak;
- selected panel seeds: `45001`, `45003`;
- executed checkpoint-lineage pairs: 12;
- module 3 screen pass: false;
- positive ecological routed output: false;
- module copy number ready: false.

The redesigned source population removed the original dominant-lineage objection for the two qualified peak checkpoints, but module 3 did not produce a routed-output effect with the same practical direction in both independent panel seeds and at least two non-dominant lineage identities.

Several total-expression or retained-cost effects were material. These cannot substitute for routed output because removing expression cost changes energy accounting even when the module output is causally inactive.

Decision:

```text
module-3-not-replicated-in-redesigned-source-population-stop-before-copy-number
```

Consequences:

- do not run the D2-H 300-tick module confirmation;
- do not duplicate, delete or reroute modules;
- retain fixed modules as background inherited structure;
- move the main line to D4 environment matching and ecological-niche tests.
