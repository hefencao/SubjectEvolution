# v0.79 governance check

## Data and task root cause

The supplied portfolio audit matches project history and contains no reconstruction error. The portfolio is genuinely exhausted, so the task changes from repairing governance state to defining a scientifically distinct causal interface.

D3-T is selected because it addresses the unresolved direct material-flow question in D3-E, not because it is the nearest unused intervention. Previous paired failures acted through policy or functional-module outputs; previous spatial-processing panels estimated movement response and alignment. D3-T instead estimates the conserved support-to-conversion interface with explicit execution cost.

## Long-term principle

When an intervention can be enabled without actual target exposure, its flag is not sufficient manipulation evidence. A candidate must preregister an exposure-weighted direct engagement measure when such a measure is available. This rule is documented in project governance and enforced for spatial-processing-support candidate specifications.

## Chat-only principles

No active principle remains only in chat. Successful verification remains concise in delivery, and every iteration still provides suggested commands.
