# v0.152 D1-Z 主体图 Stage 3C-36：几何迁移根因分解

## 类型与范围

- 类型：`[MAIN-EXP]`
- Git 标题：`[MAIN-EXP] D1-Z: decompose bootstrap geometry transport failure`
- 只读比较已冻结的原面板与独立面板 Stage 3C-25～27 输出。
- 不重跑 runtime，不替换 seed，不放宽 gate，不运行失败面板的 Stage 3C-28+。

## 冻结结果

1. 候选支持完全一致：18 个 source 均为 128 request、112 assigned、16 无候选、16 forced、96 multi-candidate，并具有相同 candidate-count histogram。
2. winner reuse 中位比例两个面板均为 0.4642857。
3. same-first-state query 从 344 降为 320；age-one 选择从 387 降为 369。
4. 仅替换 recurrence composition、保留原条件选择率时预测 367.6508，距实际 369 只有 1.3492；仅替换条件率、保留原 composition 时预测 387.3838。
5. strict-age-one 与 older winner 的 local-step 中位尺度分离仍为 200.2× 与 191.2×，没有整体坍缩。
6. 诊断 tie 从 1 增为 6，直接令 strict fraction 低于冻结的 0.99 gate；但聚合输出无法区分真实归一化方向重复、float32 量化或 1e-8 诊断容差。

## 解释边界

Stage 3C-35 的总体 age-one occupancy 下滑主要来自第一坐标 recurrence composition 改变，而不是候选数量或局部步长尺度失效。形式资格门的直接触发因素则是五个额外 near-exact tie。两者必须分开陈述。

下一步只允许在相同冻结 rank-two panel 上进行 query-level tie-origin 审计，不得先改 tie tolerance、寻址或 seed。
