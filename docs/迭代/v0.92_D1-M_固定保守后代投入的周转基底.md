# v0.92 D1-M：固定保守后代投入的周转基底

## 修复

- 统一配置 sidecar 为 `source_config.json.manifest.json`。
- 失败结果包包含配置、sidecar、阶段健康事件、总健康报告和 panel index。

## D1-L 结论

两个已运行 seed 在 tick 240 仅保留 9 和 14 个存活后代，第三个 seed
因连续健康门失败未运行。该结果只证明旧底座不合格。

## D1-M

新增所有实体相同、不可遗传的固定保守繁殖结算：0.1 事件开销、0.9
后代能量、0.8 亲代保留，总资格需求 1.8。使用三个新独立 seed，必须
同时通过 tick 120、240、360 的人口、后代、世代、founder 替换和阶段
下降约束。通过前不添加新基因、不建立配对分支。
