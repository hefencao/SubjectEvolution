# v0.143 D1-Z：主体图 Stage 3C-29 transition class 与机会条件化 Basin 审计

## 目标

在不修改运行时的前提下，排除 source-boundary 单候选强制分配，判断 recurrent basin occupancy 是否由 exact first-state transition replay 解释，或主要来自当前状态机会与主体锚定第二坐标的局部几何。

## 固定合同

- 9 个独立 source，32 实体/source，16 bootstrap subjects；
- source tick 2，branch horizon 8，exposure 3；
- Rank-2 visible coordinates `[11, 7]`；
- normalized-dot、threshold 0.8、latest/top-1；
- `edge_forward_gate` target 与 edge carrier；
- bounded delta、自动 rollback、永久保留关闭；
- Stage 3C-28 assessment checksum 与 rank-two lineage 必须通过；
- 条件化分析只使用每 source 的 96 个 multi-candidate query。

## 结果

- raw exact-transition match enrichment：`1.032～1.825×`，9/9 source > 1；
- 同当前第一状态内 enrichment：`0.908～1.165×`，中位数 `1.014`，7 source > 1、2 source < 1；
- 最高机会条件化 transition class：`up` 6 source，`stay` 3 source；
- multi-candidate query 的同状态 winner：`77.1%～85.4%`；
- 同状态内第二坐标最近候选 selection rate：`1.0`；非最近候选：`0.0`；
- selected/unselected 第二坐标局部 step mismatch 中位数比：`0.425～0.533`；
- Stage 3C-8 稳定客观坐标：0/21。

## 冻结运行链总结

Stage 3C-23 建立 rank-two visible geometry；3C-24 重建 opportunity 与 selected identity；3C-25 排除 near-zero margin 与 exact query duplication；3C-26 分离 source-boundary forcing、age 与 opportunity；3C-27 证明 age-one basin 主要来自 strict local geometry 而非 latest tie-break；3C-28 排除 global phase 与 exact token recurrence，定位 subject-anchored recurrent basin；3C-29 进一步表明 raw transition enrichment 主要受当前状态机会混杂，而第二坐标局部性在机会条件化后仍保持一致。

## 结论边界

该链冻结为 fixed-bootstrap addressing geometry diagnosis。不得据此加入 age penalty、随机 allocation、learned weight 或 permanent retention，也不得宣称 value、causal credit、learning、通用注意力或 subjecthood。

## 轻量治理检查

- 数据与预期不一致的根因：raw transition enrichment 在未控制当前状态时看似强，但同状态条件化后接近中性；任务未偏离，Stage 3C-29 正好完成该混杂分解。
- 新长期原则：无。checksum-bound lineage、最高独立 replicate 与不把工程局部性解释为 value/learning 均已存在于治理和协议中。
- 聊天中孤立原则：无。本轮新增结论、冻结链和禁止推论已写入 status、architecture、scientific issues、decision protocol 与本迭代记录。
