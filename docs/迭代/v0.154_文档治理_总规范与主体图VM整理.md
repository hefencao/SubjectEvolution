# v0.154 文档治理：总规范与 Subject Graph VM 整理

Git title:

```text
[DOC-GOV] docs: separate charter, governance, and Subject VM authority
```

## Scope

This iteration changes documentation authority only. It does not modify runtime, configuration,
checkpoint schema, scientific workflows, source panels, frozen results, or Stage 3C
interpretation.

## Problems corrected

1. `PROJECT_CHARTER.md` mixed long-lived mission, current implementation state, duplicate section
   numbering, appended special rules, and a universal fixed seed-count requirement.
2. `PROJECT_GOVERNANCE.md` remained an append-only sequence of per-version checks rather than a
   durable cross-version rule set.
3. `PARTITIONED_SUBJECT_GRAPH_VM.md` combined current mechanism semantics with a v0.114–v0.147
   experiment diary and displayed an obsolete project status.
4. `AGENTS.md` and `ARCHITECTURE.md` did not explicitly assign authority to the charter and the
   detailed VM contract.

## Resulting authority

- Charter: mission, programme order, ontology, and project-level interpretation limits.
- Governance: durable inference, workflow, identity, and documentation rules.
- VM document: current mechanism, ownership, phase, safety, and capability contract.
- Result ledger: frozen Stage 3C scientific chronology.
- Status and issues: current queue and unresolved questions only.

The original three documents are preserved under `docs/history/v0.153/` as explicitly
non-normative audit snapshots.

## Charter review decision

The project mission and causal programme remain valid. The following stale or misplaced content
was changed:

- removed current-stage implementation claims from the charter;
- removed the universal “10 exploratory / 30 core” seed requirement;
- replaced it with protocol-specific support justification using independent source histories;
- consolidated health, environment-timescale, physical value-chain, conflict, epoch, and
  multi-channel evidence principles into correctly numbered durable sections;
- clarified that Subject Graph VM architecture is an allowed prior but its content is not.

## Scientific status

Stage 3C-37 remains frozen. Stage 3C-38 remains the next authorized mainline experiment. No new
scientific claim is made in this iteration.
