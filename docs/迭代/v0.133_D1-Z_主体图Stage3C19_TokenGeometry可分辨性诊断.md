# v0.133 D1-Z：主体图 Stage 3C-19 Token Geometry 可分辨性诊断

## 1. 目标

Stage 3C-18 的 top-2 只增加重复引用，所有 selected similarity 仍为 1.0。本轮不修改任何运行机制，只诊断 normalized-dot scorer 实际可见的 token geometry 是否具有方差和 score 可分离性。

## 2. 固定合同

九个 source `12301..12309`，每 source 32 实体、16 bootstrap subject、source tick 2、branch horizon 8、exposure 3、CPU、`edge_forward_gate` carrier-on、latest、top-1、bounded delta、自动回滚和无永久保留全部保持不变。只读取 read-only-control checkpoint。

## 3. 结果

association request port 0 与 modulation controls 1-28 被排除，实际可见 ports 为 29、30、31。九 source 共 1152 个 token，全部精确为 `[0,0,1]`。每 source coordinate variance 全为 0，centered covariance numerical/effective rank 为 0，uncentered direction rank/effective rank 为 1。

在 delay 1..6 内共有 3888 个合法 query/candidate pair，全部为精确相同向量，normalized-dot score 全为 1.0；1008 个有候选 query 的最佳 score 对 threshold 0.8 的 margin 固定为 0.2，864 个多候选 query 的 best-second spread 全为 0。

## 4. 解释边界

当前 score 没有内容区分力，Stage 3C-17/18 的 allocation 差异由固定时间 tie-break 与 candidate limit 决定。该事实只适用于当前 fixed bootstrap readout 和 operating point；不证明一般 token、学习、主体性或通用注意力不可能，也不授权 retention、reward、learned weight 或扩大 top-k。

## 5. 下一步

下一最小边界是对 ports 29-31 的现有 role-neutral graph readout 做可达性/局部灵敏度诊断，一次只改变一个 readout 或 operating context。不得同时改变 similarity、allocator、delta、retention 或完整注意架构。
