# v0.142 D1-Z：主体图 Stage 3C-28 离散状态与主体锚定 Basin 审计

## 目标

在不修改运行时的前提下，判断 Stage 3C-27 的 recurrent winner basin 是跨主体同步采样 phase、完整 token 精确复现，还是主体内共享离散状态与慢变量共同形成的几何 basin。

## 固定合同

- 9 个独立 source，32 实体/source，16 bootstrap subjects；
- source tick 2，branch horizon 8，exposure 3；
- Rank-2 visible coordinates `[11, 7]`；
- normalized-dot、threshold 0.8、latest/top-1；
- `edge_forward_gate` target 与 edge carrier；
- bounded delta、自动 rollback、永久保留关闭。

## 结果

- 九 source 共同第一坐标值：`0.477777809`、`0.588888884`、`0.699999988`；
- same-phase transition agreement 相对 cross-phase 基线差值：`-0.0244～0.0286`，不支持全局同步 phase；
- 第二坐标 ICC：`0.9966～0.9988`，绝对步长中位数约 `4.08e-4～5.39e-4`；
- 同第一状态候选比例：`34.7%～44.4%`；
- winner 同第一状态比例：`67.9%～80.4%`，enrichment `1.80～2.08×`；
- `86.6%～96.4%` query 仍有不同第一状态候选；
- 同状态 winner 在 9/9 source 中始终是该状态内第二坐标最近候选；
- 完整 visible token 精确重复 winner：0；
- Stage 3C-8 稳定客观坐标：0/21。

## 结论边界

共享离散码本不等于跨主体同步相位。当前 winner 复用更符合主体内离散状态与主体特异慢第二坐标形成的 recurrent geometric basin，不能解释为正确 causal credit、学习、价值或通用注意力。下一步继续只读审计 transition class 与机会条件化后的 basin occupancy。
