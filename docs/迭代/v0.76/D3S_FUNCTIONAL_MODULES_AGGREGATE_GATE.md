# D3-S functional-modules aggregate harvest gate

Candidate: `functional-modules-harvest-acute-effect-v1`

D3-R confirmed that the bounded physiology-output path was engaged but did not reach its preregistered practical-effect threshold. The current family funnel therefore permits one aggregate gate before any further bounded functional-module candidate.

D3-S neutralizes the complete functional-module phenotype while retaining inherited coordinates and structural costs. Its primary estimand is the seed-level matched change in cumulative realized resource harvest over 120 ticks.

A panel is eligible only when the baseline has non-zero functional-module harvest residual use and the intervention branch records complete module ablation with zero changed-entity fraction and zero residual effective dimensions.

The practical-effect threshold is 2%. A manipulation-confirmed terminal negative closes `functional-modules` revision 1. A positive screen may advance only to disjoint-seed replication. Neither outcome authorizes a long-horizon selection claim or interpretation of individual module slots.
