# v0.157 D1-Z：主体图 Stage 3C-39 动作边界机会迁移审计

## 任务身份

- 类型：`[MAIN-EXP]`
- Git 标题：`[MAIN-EXP] D1-Z: audit action-boundary opportunity transport across panels`
- 工作流：`SCIENTIFIC-FREEZE` + `RELEASE-HANDOFF`
- 运行时变化：无

## 问题

原 panel 的 seed 12305、12308 发生 alignment-specific sampled-action crossing，而独立 panel 的九个 source 都有 continuous Subject VM divergence 却没有 crossing。本轮只读比较两个冻结 panel，检查差异是否可由 divergence 幅度、时程、已选 action 概率变化或 bootstrap 机会结构解释。

## 冻结数据

- 原 panel continuous divergence event：129；独立 panel：123。
- potential L1 mean 的 source 中位数：0.0709219 与 0.0686553。
- selected-action probability 绝对变化 mean 的 source 中位数：0.00328351 与 0.00348256。
- tick 10 及以后 divergence 占比：0.5426 与 0.5772。
- 所有观测幅度指标的跨 panel 范围均重叠。
- 不存在一个单调高阈值，可把原 panel 的两个 crossing source 与全部非 crossing source完全分开。

## 解释

独立 panel 的零 crossing 不是因为 continuous divergence 普遍更弱、发生更早、已选 action 概率变化更小，或 candidate/local token geometry 未迁移。first-state recurrence composition 确实不同，但当前事件证据不能证明它直接导致零 crossing。

冻结 trace 缺少完整 masked policy logits 与 counter-based categorical draw，因此无法重建所有 action 的竞争关系或精确随机采样边界。根因被收窄到该不可见状态，但尚未解析。

## 下一边界

下一项必须是 `[ENGINEERING]` 语义中立导出 instrumentation；不得继续增加 panel、延长 exposure、修改 crossing definition 或建立后见幅度阈值。Stage 3C-40 在 instrumentation 证明 sampled action 与 random stream 不变前保持阻塞。
