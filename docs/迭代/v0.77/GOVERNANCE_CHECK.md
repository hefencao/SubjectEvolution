# v0.77 governance check

## Data and task root cause

The D3-S sample is valid: 8/8 seeds are eligible and 8/8 satisfy the complete-ablation contract. The effect is directionally consistent but practically small. The current task is therefore adjusted from another nearby component candidate to a scientific revision step.

## Long-term principles

The existing rule that a closed family may reopen only through a new directly measurable interface is strengthened from documentation to executable validation. A new durable rule is added: portfolio exhaustion does not authorize automatic replacement-candidate selection.

## Chat-only principles

The per-iteration governance check requested by the project owner is now stored in `docs/PROJECT_GOVERNANCE.md`. No newly active principle remains only in chat after this iteration.
