# Subject Evolution v0.29

v0.29 是结构测量协议显式化版本，不改变默认世界动力学。

## 输入结果

- 18 unique anchors；
- 108/108 eligible event-timed pairs；
- 0 pairing failures；
- transfer-off 的局部文化状态维持方向跨三类事件重复；
- group refresh 的 current-label cohesion 方向主要由评价边界定义产生；
- policy、memory、Top-k 与 affinity 的下游方向依赖事件类型。

## 实现

- `trusted-directed-fixed-round-min-label-v1`；
- `normalized-fixed-count-grid-v1`；
- `natural-event-paired-intervention-matrix-v2`；
- `exposure-only-local-peak-selection-v2`；
- `multi-seed-long-run-analysis-v8`；
- `subject_evolution.protocol_audit`；
- mixed physical region geometry 默认拒绝；
- build-system 显式依赖 `wheel`。

## 文件

- `GROUP_REGION_ANCHOR_PROTOCOLS.md`：三类策略的规范说明；
- `SPATIAL_PARTITION_IMPLEMENTATION.md`：统一 region mapping 与哈希；
- `INPUT_ANALYSES_ASSESSMENT.md`：用户结果审计；
- `natural_event_result_synthesis.md`：108 pairs 综合；
- `protocol_audit.md/json`：旗舰配置协议实例；
- `PATCH_NOTES.md`：升级说明；
- compatibility、test、packaging reports：发行验证。
